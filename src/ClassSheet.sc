val sanDiegoBicentennial = new com.illumina.scala.survey.ClassSyntax(new java.util.GregorianCalendar(1969, 7, 16).getTime)
val today = new com.illumina.scala.survey.ClassSyntax

//Use def to create methods.  The val keyword is legal but will evaluate only once and immediately
today.timeToMastery

// object = singleton (notice no new keyword)
val os = com.illumina.scala.survey.ObjectSyntax
os.timeToMastery
